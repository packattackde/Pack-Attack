# Copy to .neon.env (gitignored) and fill in. Used by: npm run neon:dev-branch
# API key: https://console.neon.tech/app/settings/api-keys
# Project ID: Neon console → open project → Settings, or from dashboard URL
NEON_API_KEY=
NEON_PROJECT_ID=
